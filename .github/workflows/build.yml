name: Build EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore
      working-directory: ./KaliteKontrolApp
      run: dotnet restore

    - name: Publish EXE
      working-directory: ./KaliteKontrolApp
      run: dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:PublishTrimmed=false -p:PublishReadyToRun=false -o ../publish

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: KaliteKontrolApp_EXE
        path: ./publish
